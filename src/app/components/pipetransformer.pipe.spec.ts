import { PipetransformerPipe } from './pipetransformer.pipe';

describe('PipetransformerPipe', () => {
  it('create an instance', () => {
    const pipe = new PipetransformerPipe();
    expect(pipe).toBeTruthy();
  });
});
